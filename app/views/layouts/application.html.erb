<!DOCTYPE html>
<html>
  <head>
    <title>R7Additional</title>
    <meta name="viewport" content="width=device-width,initial-scale=1" />
    <%= csrf_meta_tags %> <%= csp_meta_tag %> <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %> 
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <div
      class="p-5 text-body-secondary-emphasis text-wrap rounded banner-background"
    >
      <div class="container text-cm-center pt-5">
        <h1 class="text-center font-size-1 fw-bold">Customer-Orders</h1>
        <p class="text-center font-size-2 fw-bold">
          keep your shopping experience under control
        </p>
      </div>
    </div>
     <% if flash[:notice].present? %>
      <div class="alert alert-light" role="alert">
        <p style="color: green"><%= flash[:notice] %></p>
      </div>
    <% end %>
    <% if flash[:alert].present? %>
      <div class="alert alert-danger" role="alert">
        <p class="my_alert"><%= flash[:alert] %></p>
      </div>
    <% end %>

    <nav class="navbar navbar-expand-lg bg-body-tertiary nav">
      <div class="container-fluid">
        <div class="collapse navbar-collapse" id="navbarNavDropdown">
          <ul class="navbar-nav gap-2">
            <li class="nav-item">
              <div class="btn-group" role="group">
                <button
                  type="button"
                  class="btn btn-secondary dropdown-toggle"
                  data-bs-toggle="dropdown"
                  aria-expanded="false"
                >
                  Customers
                </button>
                <ul class="dropdown-menu">
                  <li><%= link_to "To customers", customers_path%></li>
                  <div class="dropdown-divider"></div>
                  <% Customer.all.each do |customer| %>
                  <li>
                    <%= link_to customer.full_name, customer_path(customer)%>
                  </li>
                  <% end %>
                </ul>
              </div>
            </li>
            <li class="nav-item">
              <div class="btn-group" role="group">
                <button
                  type="button"
                  class="btn btn-secondary dropdown-toggle"
                  data-bs-toggle="dropdown"
                  aria-expanded="false"
                >
                  Orders
                </button>
                <ul class="dropdown-menu">
                  <li><%= link_to "To orders", orders_path%></li>
                  <div class="dropdown-divider"></div>
                  <% Order.all.each do |order| %>
                  <li><%= link_to order.product_name, order_path(order)%></li>
                  <% end %>
                </ul>
              </div>
            </li>
          </ul>
        </div>
      </div>
    </nav>
    <div "container-fluid">
      <%= yield %>
    </div>
  </body>
</html>
